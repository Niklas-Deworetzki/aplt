- preservation proof
- replace the Maybe in Check with Either String
- exercise 4.1 is now not implemented at all, it is only inlined. 
- Guard doesn't use the type, which is weird

weird stuff I encountered:
- subst is used for type substitution where is the term substitution? 
- I didn't understand what matchLabels does.
- variable types have distributions. Is that correct?  Maybe I can test this by using an interesting recursive datatype? 
-- Evan's advice : have two different types of quantification: quantification over arbitrary types, and quantification over distributable types. 
-- JP: either solution is fine as long as you can reason about it and learn from the project. 


- in the pdf, what does the intersection symbol mean in the dynamics of Plus? 
- synthesizing and inference had some interplay right?
- in synthesizing AppT, I think the roles of the arguments were switched (this also occurs in convertExp)


- 
